const express = require('express');
const router = express.Router();

// Placeholder for other route files

// Telemedicine routes
router.get('/sessions', (req, res) => {
  res.json({ message: 'Telemedicine sessions endpoint - To be implemented' });
});

module.exports = router;